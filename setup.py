import platform
import sys

from setuptools import find_packages, setup

setup(name="subsequent",
      version="0.0.1",
      description="Imagine A Deep Network That Performs Successive Cheap Queries On Its Input",
      author="Marcus Lewis",
      url="https://github.com/mrcslws/subsequent",
      packages=find_packages(),
)
